https://practice.geeksforgeeks.org/problems/queue-designer/0/?track=dsa-workshop-1-queue&batchId=308

void _pop(queue<int>s);


 // } Driver Code Ends
//User function Template for C++


//User function Template for C++

queue<int>_push(int arr[],int n)
{
    queue<int>s;
   for(int i=0;i<n;i++)
   s.push(arr[i]);
   return s;
}

void _pop(queue<int>s)
{
     while(!s.empty())
    {
        cout<<s.front()<<" ";
        s.pop();
    }
} 
